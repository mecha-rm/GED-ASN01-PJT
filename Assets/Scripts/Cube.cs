using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// level editor namespace
namespace GED
{
    public class Cube : GED.Object
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}